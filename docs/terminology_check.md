# 术语一致性检查（成员3）

目标：确保文档与代码中对表、接口、字段和模块的命名一致，便于合并与评分。

示例不一致项及建议修正：

| 文档中术语 | 代码中名称 | 建议修正（统一为） |
|---|---:|---|
| 课程目标 / 考试目标 | `study_targets`（表） | 文档中统一使用“课程/考试目标（`study_targets`）”|
| 资料 | `materials`（表） | 文档使用“资料（materials 表）”，并在接口示例中标注 `parse_status` 字段含义 |
| AI 调用记录 | `ai_call_logs`（规划） | 在文档中标注为“规划表：`ai_call_logs`，尚未实现” |
| 问答记录 | `qa_records`（规划） | 文档与代码均使用 `qa_records` 作为术语 |

操作步骤（建议）：
1. 将 `docs/member3_system_design.md` 的术语替换为与上表一致的形式。
2. 在 `中期.md` 中粘贴章节时，保留括号内的代码表名，避免造成歧义。
3. 若决定更改代码命名，先在仓库中统一提交重命名 PR，并更新所有引用。

