# 后端开发成员 A Todo List

## 阶段目标

中期验收后，开发成员 A 的重点从“完成基础平台与资料链路”转向“稳定接口契约、补齐管理员能力、完善测试和期末演示支撑”。

成员 A 继续负责：

- 账号体系与权限校验
- 课程/考试目标管理
- 资料上传、资料管理与资料解析
- 管理员端基础资源查看
- 向 AI 学习闭环模块提供稳定的资料数据能力

## 1. 管理员端基础能力

- [x] 新增管理员路由模块 `app/routers/admin.py`。
- [x] 在 `app/main.py` 中注册管理员路由。
- [x] 实现 `GET /admin/users` 管理员查看用户列表接口。
- [x] 实现 `GET /admin/materials` 管理员查看全部资料列表接口。
- [x] 实现 `GET /admin/tasks?status=failed` 查看资料解析失败任务接口。
- [x] 实现 `POST /admin/tasks/{id}/retry` 失败资料重新解析接口。
- [x] 所有管理员接口接入 `get_current_admin_user()`，确保只有管理员可访问。
- [x] 管理员列表接口统一使用 `items`、`total`、`page`、`page_size` 分页结构。
- [x] 管理员接口统一使用 `code`、`message`、`data` 响应结构。

## 2. 资料上传与解析链路优化

- [x] 明确资料上传后是否自动解析，统一前端调用流程。
- [x] 保留手动解析接口 `POST /materials/{material_id}/parse`，用于失败重试和兼容调试。
- [x] 采用自动解析，在上传成功后触发解析流程并返回最新解析状态。
- [x] 优化重新解析逻辑，重新解析时清空旧的 `parsed_text` 和 `parse_error`。
- [x] 解析开始时将 `parse_status` 更新为 `parsing`。
- [x] 解析成功后保存 `parsed_text`，并将 `parse_status` 更新为 `parsed`。
- [x] 解析失败后保存 `parse_error`，并将 `parse_status` 更新为 `failed`。
- [x] 保证删除资料后，普通用户列表和详情接口不再返回该资料。
- [x] 检查资料归属校验，防止用户访问或解析他人资料。

## 3. PDF 与 OCR 能力推进

- [x] 将 PDF 文本提取从 Mock 逐步替换为真实解析实现。
- [x] 对无法提取文本的 PDF 返回明确失败原因。
- [x] 图片 OCR 接入 Tesseract，支持简体中文和英文混合识别。
- [x] 扫描版 PDF 增加 OCR 兜底：文本提取为空时转图片识别。
- [x] 记录不同文件类型的解析策略：TXT 真实读取、PDF 文本提取与 OCR 兜底、图片 OCR。

## 4. 与成员 B 的数据契约

- [x] 确认 `material_id` 可作为 AI 模块读取资料的稳定入口。
- [x] 确认 `target_id` 可用于关联课程/考试目标。
- [x] 确认 `parse_status` 状态值统一为 `uploaded`、`parsing`、`parsed`、`failed`。
- [x] 确认只有 `parse_status == "parsed"` 且 `parsed_text` 非空时，AI 模块才继续处理。
- [x] 更新 `docs/material-contract-for-ai.md`，保持文档与代码一致。
- [ ] 和成员 B 联调知识提炼、AI 问答、AI 出题对资料字段的读取逻辑。

## 5. 自动化测试补充

- [x] 补充用户注册接口测试。
- [x] 补充用户登录接口测试。
- [x] 补充当前登录用户信息接口测试。
- [x] 补充学生与管理员权限校验测试。
- [x] 补充课程/考试目标创建测试。
- [x] 补充课程/考试目标列表、详情、修改、删除测试。
- [x] 补充资料上传成功测试。
- [x] 补充资料上传文件类型校验测试。
- [x] 补充资料上传文件大小校验测试。
- [x] 补充资料列表、详情、预览、删除测试。
- [x] 补充资料解析成功测试。
- [x] 补充资料解析失败测试。
- [x] 补充资料重新解析测试。
- [x] 补充管理员用户列表测试。
- [x] 补充管理员资料列表测试。
- [x] 补充管理员失败任务查看与重试测试。

## 6. 文档更新

- [x] 更新 `README.md`，移除“资料模块尚未完成”的旧说明。
- [x] 在 `README.md` 中补充资料上传接口示例。
- [x] 在 `README.md` 中补充资料解析接口示例。
- [x] 在 `README.md` 中补充资料预览接口示例。
- [x] 在 `README.md` 中补充管理员接口示例。
- [x] 更新资料解析状态说明。
- [x] 更新 Docker 启动、数据库迁移和上传目录说明。
- [x] 确认接口文档与实际 FastAPI Swagger 展示一致。

## 7. 期末演示准备

- [ ] 准备一个学生测试账号。
- [ ] 准备一个管理员测试账号。
- [ ] 准备至少一个课程/考试目标。
- [ ] 准备至少一份已解析成功的资料。
- [ ] 准备至少一份解析失败的资料，用于展示失败任务和重试流程。
- [ ] 确认学生端流程可演示：登录 -> 创建目标 -> 上传资料 -> 解析资料 -> 查看资料状态。
- [ ] 确认管理员端流程可演示：登录管理员 -> 查看用户 -> 查看资料 -> 查看失败任务 -> 触发重试。
- [ ] 确认 Docker Compose 启动流程可稳定复现。
- [ ] 确认 Alembic 数据库迁移可稳定执行。

## 8. 建议优先级

### P0 必须完成

- [x] 管理员用户列表接口。
- [x] 管理员资料列表接口。
- [x] 资料解析失败任务查看接口。
- [x] 资料重新解析接口。
- [x] 管理员权限校验。
- [x] A 模块核心接口测试。
- [x] README 与当前接口状态同步。

### P1 应尽量完成

- [x] PDF 真实文本提取。
- [x] 更完整的资料解析异常处理。
- [ ] 与成员 B 完成资料契约联调。
- [ ] 期末演示数据准备。

### P2 时间允许再做

- [x] 图片 OCR 真实接入。
- [x] 解析任务异步化。
- [x] 管理员操作日志。
- [x] 更复杂的资料处理队列和任务状态追踪。
